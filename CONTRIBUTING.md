# Contribution Guide

## 工作流程

1. 從最新 `main` 建立短期分支：`feat/<scope>`、`fix/<scope>`、`docs/<scope>` 或 `content/<scope>`。
2. 不把原始題庫、未授權資料或任何秘密放進工作樹的可追蹤路徑。
3. 完成公開資料檢查、內容驗證與相關測試。
4. 使用 PR template 說明範圍、資料風險、QA 與回復方法。
5. CI 全綠、對話解決並取得必要審核後才合併。

禁止直接推送 `main`、force push、重用 build number，或以測試資料覆蓋使用者學習紀錄。
